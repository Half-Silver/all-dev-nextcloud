#!/bin/sh -e

run-snap-migrations
# Control Tower: the update finished (update_complete). Best-effort.
if [ -x "$SNAP/bin/ct-engine-run" ]; then
	"$SNAP/bin/ct-engine-run" hook-post-refresh || true
fi
